function Header() {
  return (
    <header className="header">
      <h2>MovieZone</h2>

      <nav>
        <a href="#">Inicio</a>
        <a href="#">Películas</a>
        <a href="#">Favoritas</a>
        <a href="#">Contacto</a>
      </nav>
    </header>
  );
}

export default Header;